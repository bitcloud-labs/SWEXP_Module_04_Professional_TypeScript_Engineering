/**
 * Lab 00 — Toolchain setup. See README.md.
 * Implement greet so the test goes from red to green.
 */
export function greet(name: string): string {
  // TODO: return `Hi, NAME` with NAME uppercased — e.g. greet('forge') -> 'Hi, FORGE'.
  return '';
}
